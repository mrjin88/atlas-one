# render_images.ps1

Purpose:
Create the LA-0001 image render queue and required folders.

Install:
Copy this file to:

E:\atlas-one\tools\render\render_images.ps1

Run:

```powershell
cd E:\atlas-one
.\tools\render\render_images.ps1
```

Optional:

```powershell
.\tools\render\render_images.ps1 -ProjectPath "E:\atlas-one\projects\lost-archive\content\LA-0001-roman-concrete"
```
